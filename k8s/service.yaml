apiVersion: v1
kind: Service
metadata:
  name: regime-predictor
  namespace: default
  labels:
    app: regime-predictor
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 8000
    targetPort: 8000
    protocol: TCP
  selector:
    app: regime-predictor
